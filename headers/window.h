#include "viewport.h"
class Window{
    protected:
        Viewport *viewport = nullptr;
    private:
        int dimensionX = 80;
        int dimensionY = 20;
        int statusX = 80;
        int statusY = 15;
    
};